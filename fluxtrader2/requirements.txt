# fluxtrader2 analysis image. CPU only. Keep this list short: every phase that needs a new
# library adds it here with a one-line reason, and the image is rebuilt (scripts/ft2.sh).
numpy>=1.26,<3
pandas>=2.1,<3
pyarrow>=14
scipy>=1.11,<2
scikit-learn>=1.4,<2      # ridge / logistic baselines, rank transforms (PLAN P2, P5)
statsmodels>=0.14,<1      # variance-ratio, Ljung-Box, HAC standard errors (PLAN P2)
lightgbm>=4.1,<5          # shallow boosted trees (PLAN P2, P5); needs libgomp1 in the image
tabulate>=0.9              # DataFrame.to_markdown for the inventory report (P0)
